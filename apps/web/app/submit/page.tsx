import { SubmitWizard } from '@/components/submit-wizard';

export const metadata = {
  title: 'Add your pathway — Precedent',
  description: 'Record the sequence of steps you took, including the parts that did not work.',
};

export default function SubmitPage() {
  return <SubmitWizard />;
}
